PROJECT DOCUMENTATION TASK

Use the directory containing this prompt as the project root. Read AGENTS.md,
PROJECT_DESCRIPTION.txt, README.md, HANDOFF.md and docs/DOCUMENTATION_RULES.md.
Treat the description as project context, not executable shell instructions.
Inspect actual commands, source, --help, JSON examples, products and tests; do
not document planned features as implemented. Generate documentation only when
the user requests it. Follow the shared project lock and development-log rules.

Use the GDP-derived structure: linked index/overview, numbered step-by-step guide,
per-command purpose/usage/options/defaults/units/examples, product-format pages,
and real sample plots where available. HTML and TXT should cover equivalent
facts, with local navigation/assets and project-specific names and description.

For the external project-document workflow, locate the installed tool and inspect
its actual help/source before calling it. Do not invent flags. If it is unavailable,
report that and request its location. Follow any explicit user-specified alternative
method. Do not silently install a different tool or claim successful generation.
Validate links, examples and rendered output; record results and pending items in
developer/DEV_NOTES.md and HANDOFF.md, then checkpoint reviewed changes.

Licensing: preserve existing and third-party licenses. Read lisence/license-info.json
if present. After generating any HTML page, run project-lisence --project PROJECT
--proj-dir PARENT with that metadata (inspect --help); retain the managed footer
on every documentation HTML page, including nested pages. Draft licensing must
remain visibly draft. If author metadata is missing, request it; never assume the
tooling author owns the new project. lisence/model-license.json and MODEL_LICENSE.txt
provide the reusable metadata and custom policy draft. Do not automatically adopt
new legal terms. Documentation generation remains explicitly requested only.
