Illustrative configurations only; example.fits is not supplied.
output_path and plot_path are directories. Parameters shown are examples, not an astronomy algorithm specification. Replace them with documented parameters for the implemented command.
